---
'agent-browser': patch
---

Add native HAR file generation with `har start` and `har stop <path.har>`.

This replaces the previous HAR stub behavior with Playwright-backed recording,
adds CLI command support, and updates docs/skill references for HAR workflows.
